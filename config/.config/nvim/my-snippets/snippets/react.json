{
  "typescriptReactFunctionalComponent": {
    "prefix": "tsr",
    "body": [
      "interface ${1}Props {${2}}",
      "",
      "export default function ${1:${TM_FILENAME_BASE}}({${0}}: ${1}Props) {",
      "  return (",
      "    <div>${1:first}</div>",
      "  )",
      "}"
    ],
    "description": "Creates a React Functional Component with ES7 module system and TypeScript interface"
  }
}
